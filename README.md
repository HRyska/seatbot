# 🪑 Seat Bot - Бот для бронирования мест в офисе

Telegram-бот для управления бронированием рабочих мест в офисе с административной панелью.

## 📋 Содержание
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Структура базы данных](#структура-базы-данных)
- [Блок-схемы](#блок-схемы)
- [API и состояния](#api-и-состояния)
- [Логирование](#логирование)
- [Администрирование](#администрирование)

---

## 🎯 Возможности

### Для пользователей:
- 📅 **Бронирование места** - визуальный календарь + карта офиса
- 👀 **Мои брони** - просмотр активных броней
- ❌ **Отмена брони** - отмена своих броней
- 🔁 **Изменение брони** - перенос на другую дату/место

### Для администраторов:
- 📋 **Все брони** - просмотр всех активных броней
- 🗑️ **Отмена брони пользователя** - по ID или username
- ➕ **Бронирование за пользователя** - создание брони от имени другого пользователя
- 🔄 **Изменение брони пользователя** - перенос чужой брони
- ❌ **Отмена всех броней** - массовое удаление (с подтверждением)
- 👤 **Управление администраторами** - добавление/удаление админов

### Защита данных:
- ✅ Один пользователь = одна бронь на дату
- ✅ Защита от двойного бронирования одного места
- ✅ Стабильные брони (фиксированные места по дням недели)
- ✅ "Мама бота" - главный админ, которого нельзя удалить

---

## 💻 Требования

- Python 3.9+
- aiogram 3.4.1+
- SQLite 3
- Telegram Bot Token

---

## 🚀 Установка

### 1. Клонирование и настройка

```bash
# Создайте папку проекта
mkdir seatbot
cd seatbot

# Скопируйте bot.py в папку
# Поместите изображение office_map.png (карта офиса)

# Создайте виртуальное окружение
python3 -m venv .venv
source .venv/bin/activate  # Linux/Mac
# или
.venv\Scripts\activate  # Windows
```

### 2. Установка зависимостей

```bash
pip install aiogram==3.4.1
```

### 3. Настройка переменных окружения

```bash
# Linux/Mac
export BOT_TOKEN="your_bot_token_here"

# Или добавьте в ~/.zshrc (Mac) / ~/.bashrc (Linux)
echo 'export BOT_TOKEN="your_bot_token_here"' >> ~/.zshrc
source ~/.zshrc
```

### 4. Запуск

```bash
python3 bot.py
```

---

## ⚙️ Конфигурация

### Основные константы (в bot.py):

```python
OFFICE_MAP_PATH = "office_map.png"  # Путь к карте офиса
TOTAL_PLACES = 13                    # Количество мест

SUPER_ADMIN_ID = 528599224           # ID главного админа ("мама бота")
ADMIN_IDS = [SUPER_ADMIN_ID]        # Список ID администраторов

# Стабильные брони (место: [дни недели])
PERMANENT_BOOKINGS = {
    7: [1, 3]  # Место №7 по вторникам (1) и четвергам (3)
}
```

### Дни недели:
- 0 = Понедельник
- 1 = Вторник
- 2 = Среда
- 3 = Четверг
- 4 = Пятница
- 5 = Суббота
- 6 = Воскресенье

---

## 🗄️ Структура базы данных

### Таблица `users`
```sql
telegram_id INTEGER PRIMARY KEY  -- Telegram ID пользователя
username TEXT                    -- Username в Telegram
first_name TEXT                  -- Имя пользователя
created_at TIMESTAMP            -- Дата регистрации
```

### Таблица `places`
```sql
id INTEGER PRIMARY KEY          -- ID места (1-13)
name TEXT                       -- Название (Место №X)
description TEXT                -- Описание места
```

### Таблица `bookings`
```sql
id INTEGER PRIMARY KEY          -- ID брони
user_id INTEGER                 -- ID пользователя (FK -> users)
place_id INTEGER                -- ID места (FK -> places)
booking_date DATE               -- Дата брони (ДД.ММ.ГГГГ)
status TEXT                     -- Статус: 'active' или 'cancelled'
created_at TIMESTAMP           -- Дата создания брони
```

---

## 📊 Блок-схемы

### 1. Процесс бронирования места

```
START
  ↓
Пользователь нажимает "🪑 Забронировать место"
  ↓
Бот показывает календарь
  ↓
Пользователь выбирает дату
  ↓
Проверка: Есть ли уже бронь на эту дату?
  ├─ ДА → Показать ошибку → STOP
  └─ НЕТ → Продолжить
        ↓
Проверка: Есть ли свободные места?
  ├─ НЕТ → "Все места заняты" → STOP
  └─ ДА → Показать карту офиса + список мест
        ↓
Пользователь выбирает место
  ↓
Показать подтверждение: "Место №X на ДД.ММ.ГГГГ?"
  ↓
Пользователь нажимает:
  ├─ ✅ ОК → Создать бронь
  │         ├─ Успех → "✅ Место забронировано"
  │         └─ Ошибка → "❌ Место занято" + кнопка "🔄 Выбрать другое"
  ├─ 🔁 Поменять → Вернуться к выбору места
  └─ ❌ Отмена → STOP
```

### 2. Админское бронирование за пользователя

```
START
  ↓
Админ нажимает "➕ Забронировать за пользователя"
  ↓
Админ вводит ID или @username
  ↓
Проверка: Пользователь найден?
  ├─ НЕТ → "❌ Пользователь не найден" → STOP
  └─ ДА → Показать календарь
        ↓
Админ выбирает дату
  ↓
Проверка: Есть ли свободные места?
  ├─ НЕТ → "Все места заняты" → STOP
  └─ ДА → Показать карту + список мест
        ↓
Админ выбирает место
  ↓
Показать подтверждение
  ↓
Админ нажимает ✅ ОК
  ↓
Создать бронь для целевого пользователя
  ├─ Успех → "✅ Бронь создана для пользователя X"
  └─ Ошибка → "❌ Место уже занято"
```

### 3. Защита от конфликтов

```
Попытка создать бронь
  ↓
Проверка 1: У пользователя есть бронь на эту дату?
  ├─ ДА → ОТКАЗ
  └─ НЕТ → Продолжить
        ↓
Проверка 2: Место занято на эту дату?
  ├─ ДА → ОТКАЗ
  └─ НЕТ → Продолжить
        ↓
Проверка 3: Стабильная бронь на это место/день?
  ├─ ДА → ОТКАЗ
  └─ НЕТ → СОЗДАТЬ БРОНЬ ✅
```

---

## 🔄 API и состояния (FSM)

### Пользовательские состояния

#### BookingStates (Бронирование)
- `waiting_for_date` - ожидание выбора даты
- `waiting_for_place` - ожидание выбора места
- `confirming_booking` - подтверждение брони

#### CancelStates (Отмена)
- `selecting_booking` - выбор брони для отмены

#### ChangeStates (Изменение)
- `selecting_booking` - выбор брони для изменения
- `waiting_for_new_date` - ожидание новой даты
- `waiting_for_new_place` - ожидание нового места
- `confirming_change` - подтверждение изменения

### Административные состояния

#### AdminStates
- `waiting_for_user_identifier` - ожидание ID/username пользователя
- `selecting_user_booking` - выбор брони пользователя
- `booking_for_user_date` - выбор даты для брони за пользователя
- `booking_for_user_place` - выбор места для брони за пользователя
- `booking_for_user_confirm` - подтверждение брони за пользователя
- `change_for_user_select` - выбор пользователя для изменения брони
- `change_for_user_date` - выбор брони для изменения
- `change_for_user_place` - выбор новой даты
- `change_for_user_confirm` - выбор нового места и подтверждение
- `adding_admin` - добавление администратора
- `removing_admin` - удаление администратора

### Callback Data Форматы

```python
# Календарь
"cal_{year}_{month}"           # Навигация по месяцам
"date_{DD.MM.YYYY}"           # Выбор даты
"cancel_calendar"             # Отмена выбора даты
"ignore"                      # Неактивные кнопки

# Места
"place_{place_id}"            # Выбор места (1-13)

# Брони
"booking_{booking_id}"        # Выбор конкретной брони

# Подтверждение
"confirm_yes"                 # Подтвердить
"confirm_change"              # Изменить выбор
"confirm_cancel"              # Отменить

# Админка
"admin_all_bookings"          # Показать все брони
"admin_cancel_all"            # Отменить все брони
"admin_cancel_user"           # Отменить бронь пользователя
"admin_book_for_user"         # Забронировать за пользователя
"admin_change_for_user"       # Изменить бронь пользователя
"admin_add_admin"             # Добавить администратора
"admin_remove_admin"          # Удалить администратора
"admin_cancel_action"         # Отменить админское действие
```

---

## 📝 Логирование

### Уровни логов

```python
INFO  - информационные сообщения
ERROR - ошибки выполнения
WARNING - предупреждения
```

### Примеры логов

#### Успешное бронирование:
```
INFO:__main__:User started bot: ID=123456, username=john_doe, name=John
INFO:__main__:Date selected: 15.11.2025, state: BookingStates:waiting_for_date
INFO:__main__:Place selected: 5, state: BookingStates:waiting_for_place
INFO:__main__:Confirm: state=BookingStates:confirming_booking
```

#### Админское действие:
```
INFO:__main__:User started bot: ID=528599224, username=admin, name=Admin
INFO:__main__:Date selected: 20.11.2025, state: AdminStates:booking_for_user_date
INFO:__main__:Place selected: 8, state: AdminStates:booking_for_user_place
INFO:__main__:Confirm: state=AdminStates:booking_for_user_confirm
```

#### Ошибки:
```
ERROR:__main__:Error in date selection: [описание ошибки]
ERROR:__main__:Error sending image: [описание ошибки]
```

### Мониторинг логов в реальном времени

```bash
# Linux/Mac
tail -f bot.log

# Фильтрация только ошибок
tail -f bot.log | grep ERROR

# Логирование в файл (добавьте в bot.py)
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()
    ]
)
```

---

## 👑 Администрирование

### Получение вашего Telegram ID

1. Запустите бота командой `/start`
2. Бот покажет ваш ID в сообщении: `🆔 Ваш Telegram ID: 123456789`
3. Или посмотрите в логах: `INFO:__main__:User started bot: ID=123456789`

### Добавление администратора через код

Отредактируйте `bot.py`:
```python
ADMIN_IDS = [528599224, 123456789, 987654321]  # Добавьте новые ID
```

Перезапустите бота.

### Добавление администратора через бота

1. Зайдите в ⚙️ АДМИН-ПАНЕЛЬ
2. Нажмите 👤 Добавить администратора
3. Введите Telegram ID нового админа
4. ⚠️ Перезапустите бота для применения изменений

### Удаление администратора

1. ⚙️ АДМИН-ПАНЕЛЬ → 🗑 Удалить администратора
2. Введите ID для удаления
3. ⚠️ "Маму бота" (ID: 528599224) удалить нельзя!
4. Перезапустите бота

### Управление стабильными бронями

Отредактируйте в `bot.py`:
```python
PERMANENT_BOOKINGS = {
    7: [1, 3],      # Место 7 по вторникам и четвергам
    12: [0, 2, 4],  # Место 12 по пн, ср, пт
}
```

---

## 🛠️ Troubleshooting

### Проблема: Бот не запускается

**Решение:**
```bash
# Проверьте переменную окружения
echo $BOT_TOKEN

# Проверьте установку библиотек
pip list | grep aiogram

# Проверьте версию Python
python3 --version  # Должна быть 3.9+
```

### Проблема: "BOT_TOKEN не найден"

**Решение:**
```bash
export BOT_TOKEN="your_token_here"
python3 bot.py
```

### Проблема: Карта офиса не отображается

**Решение:**
```bash
# Убедитесь, что файл существует
ls -la office_map.png

# Проверьте путь в bot.py
OFFICE_MAP_PATH = "office_map.png"
```

### Проблема: База данных повреждена

**Решение:**
```bash
# Удалите базу и перезапустите (ПОТЕРЯЕТЕ ВСЕ ДАННЫЕ!)
rm office_booking.db
python3 bot.py

# Или сделайте бэкап перед удалением
cp office_booking.db office_booking_backup.db
```

---

## 📈 Статистика и метрики

### Просмотр броней через SQLite

```bash
sqlite3 office_booking.db

# Количество активных броней
SELECT COUNT(*) FROM bookings WHERE status = 'active';

# Самые популярные места
SELECT place_id, COUNT(*) as count 
FROM bookings 
WHERE status = 'active' 
GROUP BY place_id 
ORDER BY count DESC;

# Брони на ближайшую неделю
SELECT * FROM bookings 
WHERE status = 'active' 
  AND booking_date >= date('now') 
  AND booking_date <= date('now', '+7 days')
ORDER BY booking_date;

# Выход
.quit
```

---

## 🔐 Безопасность

### Рекомендации:

1. **Не храните токен в коде** - используйте переменные окружения
2. **Регулярные бэкапы БД:**
   ```bash
   cp office_booking.db backups/office_booking_$(date +%Y%m%d).db
   ```
3. **Ограничьте доступ к серверу** - используйте SSH ключи
4. **Мониторьте логи** - проверяйте подозрительную активность
5. **Обновляйте зависимости:**
   ```bash
   pip install --upgrade aiogram
   ```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи бота
2. Убедитесь, что база данных не повреждена
3. Проверьте версии зависимостей
4. Перезапустите бота

---

## 📄 Лицензия

Проект создан для внутреннего использования в офисе.

---

## 🎉 Changelog

### v1.0 (Текущая версия)
- ✅ Базовое бронирование мест
- ✅ Визуальный календарь
- ✅ Карта офиса
- ✅ Админ-панель
- ✅ Управление администраторами
- ✅ Стабильные брони
- ✅ Защита от конфликтов
- ✅ "Мама бота" - защищённый главный админ

---

**Автор:** @Seat_Bot  
**Дата:** Ноябрь 2025